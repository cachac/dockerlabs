# 1. Practica Extra <!-- omit in TOC -->

# 2. Crea la carpeta extra
# 3. Crea el archivo extra.html
```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My First Page</title>
</head>
<body>

    <h1>Hello, World!</h1>
    <p>This is a basic HTML page.</p>



</body>
</html>
```

# 4. Crea un dockerfile para crear la imagen y ejecuta en puerto 8081
# 5. Prueba en browser IP_PUBLICA:8081

# 6. Crea un contenedor en puerto 3000 basado en la imagen: https://hub.docker.com/r/cachac/rest_api
# 7. Prueba en browser IP_PUBLICA:3000

# 8. Cambia el extra.html
```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My First Page</title>
</head>
<body>

    <h1>Hello, World!</h1>
    <p>This is a basic HTML page.</p>

    <button onclick="callService()">Call API</button>
    <pre id="result"></pre>

    <script>
    async function callService() {
      try {
        const res = await fetch("http://API_IP:3000");
        const text = await res.text();
        document.getElementById("result").textContent = text;
      } catch (err) {
        document.getElementById("result").textContent = "Error: " + err.message;
      }
    }
  </script>

</body>
</html>
```

# 9. Reconstruir la imagen y Sustituir en la línea con la palabra API_IP con la IP pública.
```
const res = await fetch("http://API_IP:3000");
```
