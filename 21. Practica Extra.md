# 1. Practica Extra <!-- omit in TOC -->

# 2. Crea la carpeta extra
# 3. Crea el archivo extra.html
```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My First Page</title>
</head>
<body>

    <h1>Hello, World!</h1>
    <p>This is a basic HTML page.</p>



</body>
</html>
```

# 4. Crea un dockerfile para crear la imagen con la version 1.0 y ejecuta en puerto 8081
# 5. Prueba en browser IP_PUBLICA:8081

# 6. Crea un contenedor en puerto 3000 basado en la imagen: https://hub.docker.com/r/cachac/rest_api
# 7. Prueba en browser IP_PUBLICA:3000

# 8. Cambia el extra.html
```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My First Page</title>
</head>
<body>

    <h1>Hello, World!</h1>
    <p>This is a basic HTML page.</p>

    <button onclick="callService()">Call API</button>
    <pre id="result"></pre>

    <script>
    async function callService() {
      try {
        const res = await fetch("http://API_IP:3000", {
          method: "POST",
          headers: {
            "Content-Type": "application/json"
					}
				});
        const text = await res.text();
        document.getElementById("result").textContent = text;
      } catch (err) {
        document.getElementById("result").textContent = "Error: " + err.message;
      }
    }
  </script>

</body>
</html>
```

# 9. Reconstruir la imagen con la version 2.0 y Sustituir en la línea con la palabra API_IP con la IP pública. Este paso debe hacerlo usando docker build con el flag --build-arg.
```
const res = await fetch("http://API_IP:3000");
```

# 10. Parte 2: Crea una app React.
## 10.1. Moverse a la carpeta dockerfiles_demo/react
## 10.2. Crea un Dockerfile con los siguientes parámetros:
- imagen: node:18-alpine
- directorio de trabajo (workdir): /app
- copia el archivo package.jso
- ejecuta el comando "npm install"
- copia el resto de archivos
- ejecuta el comando "npm run build"
- expone el puerto 3001
- ejecuta el comando de arranque (CMD) "npm start" usando la notación de array

## 10.3. Ejecuta el contenedor y publica el puerto 3001
## 10.4. Prueba en browser IP_PUBLICA:3001
### 10.4.1. Prueba el botón "Call API" y verifica que se muestra el resultado de la API.
